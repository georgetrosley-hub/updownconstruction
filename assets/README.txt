Place your logo at `assets/logo.png`.

You already provided the logo image in Cursor; since this project is plain static HTML (no build tools/binary commits),
copy your preferred logo file into this folder and name it `logo.png`.

Optional (later):
- Add project photos under `assets/projects/` and replace placeholders in `projects.html`.

